{
  "name": "volmex-mcp-server",
  "version": "0.1.0",
  "description": "MCP server exposing Volmex Finance implied-volatility index data (/history and /symbol_info) as tools, plus Volmex docs and REST API references as resources.",
  "type": "module",
  "bin": {
    "volmex-mcp-server": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "prepare": "tsc"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
